{
  "name": "cluster1-hero-validation",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "M3 validation sandbox: build a Cluster 1 (Clay Diorama) marketing hero using ONLY the moodboard's implementation.md as source-of-truth.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.4.0",
    "vite": "^5.0.0"
  }
}
